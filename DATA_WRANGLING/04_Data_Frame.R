library(dslabs)
data(murders)

df <- data.frame(
    name=c("Tanvir","ornob","ornik"),
    math_score = c(88,79,100),
    physics_score = c(100,89,90)
    )



# name, math_score,physics_score is column name
# orther vector are the values the column holding

class(df$name)


